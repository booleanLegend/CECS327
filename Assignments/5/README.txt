With a JAVA compiler installed on your machine type:
	javac <Path to UDPClient.java>
	javac <Path to UDPServer>
(Note, ensure you include the ".java" file extension at the end of the filename in the above path
To run the program, type:
	java UDPServer
	java UDPClient